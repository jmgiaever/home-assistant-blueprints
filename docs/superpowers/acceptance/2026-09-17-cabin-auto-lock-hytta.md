# Cabin auto-lock — Hytta acceptance, 2026-09-17

Blueprint sha256 b21217ffd53db462ddaabb9b88a3ce12f87cba00c039b8e2d6835cb3de06cfca (branch feat/cabin-auto-lock @ a4b58d0).
HA 2026.8.3, hass-ttlock v0.15.0, Pi 5 at 192.168.254.254. Instance: automation.hytta_auto_lock_policy.
Deployed 2026-09-17 21:28 CEST (production values). Battery values M = 2 min, P = 1 min applied for the checks below, restored in A9.
All timestamps below are UTC from the recorder (CEST = UTC + 2). Persons are UniFi Wi-Fi trackers (consider-home lag up to 5 min).

| # | Check | Expected | Measured | Result |
|---|---|---|---|---|
| A1 | Occupy latency after an unlock (passcode used instead of fingerprint) | switch off within 15 s | `unlock by passcode` 20:15:46 → `switch.joachim_cabin_auto_lock` off 20:15:47 (1 s) | PASS |
| A2 | Stale countdown (V1): does the lock still auto-lock after the arrival unlock, and does R5 undo it? | either no Auto Lock, or Auto Lock followed by an unlock within ~10 s | `Auto Lock` + lock `locked` 20:16:14 (28 s after the unlock; firmware does NOT cancel a running countdown) → R5 `unlocking` 20:16:14, `unlocked` 20:16:16 (2 s), cloud echo `unlock by gateway` 20:16:23; user confirms the door stayed open | PASS (V1: countdown not cancelled, R5 required and works) |
| A7 | Resting at H with phones on the Wi-Fi and living areas quiet (M = 2) | at H: bolt locked, auto-lock `on` 30 s, label `resting`; settle at H itself, not at the next 5-min tick | last living-area motion off 20:28:19 → helper 22:30:20 local (20:30:20Z) → `locking` 20:30:20, `locked` 20:30:23, switch `on` s=30 20:30:23, label `resting` 20:30:23; cloud echo `lock by app` 20:30:33 re-ran R2 idempotently (no extra calls). Settle fired at H to the second (20:30:00 was a safety-net tick and correctly did nothing) | PASS (also confirms final-review I1 in production) |
| A3 | Keypad lock key → vacate now | lock `locked`, switch `on` s=30, helper := now, label settles | `unlock by passcode` 20:39:32 → switch off 20:39:33 (occupy); `lock by lock key` 20:39:40 → `locked`, switch `on` s=30, helper = 22:39:40 local, label `resting`, all at 20:39:40 | PASS |
| A2b | Stale countdown after an unlock from the armed state, second observation | Auto Lock once, then R5 unlock | `unlock by passcode` 20:39:50 → switch off 20:39:50; simulated `Auto Lock` 20:39:55 → R5 `unlocking`/`unlocked` 20:39:55; real countdown `locked` 20:40:13; R5's cloud unlock executed 20:40:20 (`unlock by gateway`). Door briefly locked 20:40:13–20:40:20 (7 s) | PASS with note: cloud execution of R5 can lag ~25 s behind the command |
| A4 | Settle at H after the last living-area motion (M = 2), nobody moving | lock + arm at H ± 60 s | last living-area motion 20:40:27 → helper 22:42:28 local (20:42:28Z) → `locking` 20:42:28 (0 s after H), `locked` 20:42:29, switch `on` s=30 20:42:30; label `resting` (phones still seen by UniFi); echo `lock by app` 20:42:41 idempotent. User heard the door lock | PASS (settle exactly at H) |
| A5 | Restart mid-timer | timer survives; no settle during the 2-min post-boot hold; settle at the original H or the first safety-net tick after the hold | `docker restart` 20:44:58 with helper 22:46:29 local. HA started 20:45:19, automation initialised 20:45:20 (hold until 20:47:20). Helper restored as 22:30:20 (15 min STALE: the 10 s default stop timeout killed HA before its final restore-state dump), then pushed forward by the lock's replayed `unlock by gateway` 20:45:21 (occupy) and the user's walk (kitchen on 20:45:40, off 20:46:11 → helper 22:48:12). Settle at H: `locking` 20:48:12, `locked` 20:48:15, switch `on` s=30 20:48:16, label `resting`. No premature lock at boot | PASS, with finding: restart HA with a 60 s stop timeout (`docker restart -t 60`) so the latest helper value is dumped; a stale value fails towards locked, never open |
| A5b | Restart with `docker restart -t 60` (restore-state fidelity) | helper restored with the last value before the restart | restart issued ~20:59:15, container back 21:00:56, HA started 21:01:06; helper restored 22:54:40 local = the last value written at 20:52:39 (0 s stale); lock `locked`, switch `on` s=30, label `resting`, gateway `on` at 21:01:06–08 | PASS — use `-t 60` for HA restarts |
| — | Presence finding | — | `person.lav918` is sourced from `device_tracker.pixel_10_6` (GPS) and reads the building zone `Johanne`, not `home`; the blueprint counts only `home` as present, so Joachim is treated as away while on site. Door unaffected on Hytta (identical policies); label/hooks affected. Follow-up proposed: treat any state except `not_home`/`unavailable`/`unknown` as present | OPEN (decision pending) |
| A8 | Resting label via the bedroom, then vacant via the safety net, no lock calls | `resting` at the first tick after bedroom motion; `vacant` once the bedroom is quiet ≥ M and nobody is present; lock/switch untouched | bedroom on 21:03:52, off 21:05:00; tick 21:05:00 kept `resting` (bedroom changed 68 s earlier); tick 21:10:00 → `vacant` (bedroom quiet 5 min, Joachim's GPS in zone `Johanne` ≠ home, Lukas `not_home`); lock `locked`/switch `on` s=30 unchanged since 21:01:08; later ticks 21:15–21:30 no changes | PASS |
| — | Presence finding 2 | — | 21:10:36 `person.lav918` hopped GPS zone `Johanne` → `Stabburet` (both tiny building zones on the site); R0 treated it as a presence change: helper pushed by P (23:11:37), label `occupied` for 61 s, then the settle at H re-applied `vacant` with no lock calls. With P = 20 in production, GPS jitter between building zones would repeatedly postpone settles | OPEN (decision pending, see follow-up proposal) |
| A6 | Passage window end | lock at window end | not applicable: no passage-mode schedule configured on the lock during the battery (`binary_sensor.joachim_cabin_passage_mode` off throughout) | N/A |
| A9 | Restore production values (M = 45, P = 20) | instance values restored, automation on, entities available, helper intact | `deploy-hytta.sh prod --restart` (`docker restart -t 60`) issued 21:31:19; HA started 21:32:08; automations.yaml has `motion_delay: 45`, `presence_delay: 20`; `automation.hytta_auto_lock_policy` on 21:32:09; helper restored 23:11:37 local (unchanged since its last write at 21:10:36); lock `locked`, switch `on` s=30, gateway `on`, label `resting` at 21:32:08–10; no lock calls at boot | PASS |

## Summary

All door rules verified on the live lock: occupy 1 s, settles at H to the second, stale-countdown undo works (cloud lag up to ~25 s), explicit lock and restart behave as designed. Ops finding: restart HA with `docker restart -t 60`. Two presence findings are open for a design decision (building zones ≠ `home`; zone-to-zone moves push the timer by P).

## Redeploy 2026-09-18 (Task 14, D27: presence = inside the home zone)

Blueprint sha256 f49c530f3813e7aca91cd952eb2d00b1030a4789764fb241341395ff3e254fa4 (branch feat/cabin-auto-lock @ 3ec18e6; 154 tests, 15/15 mutations killed, ruff clean, task review approved with minor notes applied).
Before the redeploy the TTLock integration had been failing its poll since 16:17:42 CEST ("Failed to re-verify lock state"; gateway on the LAN, cloud reachable), leaving the lock, switch and last-trigger sensor `unavailable` for six hours with the door last known `unlocked`/auto-lock `off` after a 12:26 passcode unlock; it recovered at 22:17:43 after the user power-cycled the gateway, with the same states.

| # | Check | Expected | Measured | Result |
|---|---|---|---|---|
| A9b | Redeploy with `deploy-hytta.sh prod --restart` (`docker restart -t 60`) | blueprint hash matches, values M = 45 / P = 20, automation on, entities available, helper restored intact, no lock calls at boot | deploy 22:29:33–22:30:08; new boot 22:30:18; device blueprint sha256 f49c530f…; `motion_delay: 45`, `presence_delay: 20`; backup `automations.yaml.bak-20260918-202934`; automation on 22:30:20; helper restored 23:03:24 (its value since 22:18:23, 0 s stale); lock `unlocked`, switch `off`, last trigger `unlock by passcode` (all unchanged from before the restart); persons `home` (`in_zones` = [zone.home]) / `not_home`; label `occupied`; no ttlock/automation/template errors since boot | PASS |
| A10 | Label `resting` (not `vacant`) at a settle while Joachim's person reads a building zone | at the first settle/safety-net tick with the person in a nested zone and the living areas quiet: label `resting`, no lock call beyond the resting policy | 2026-09-20 02:04:55: settle at H (pushed to 02:04:55 by the 01:44:54 hop Joachim → Stabburet) with `person.lav918` = `Stabburet` (`in_zones` includes `zone.home`), Lukas `not_home`, bedroom last changed 01:04:24 (60 min earlier, so `resting_recent` false), living areas quiet since 01:08:21 → label `resting`; lock, switch and last trigger unchanged ±2 min (nothing to strengthen). Under the pre-D27 rule this settle would have read `vacant` (as A8 did) | PASS |
| A11 | A move between two zones inside the home zone pushes H by P = 20 min | helper := max(H, hop + 20 min) on the hop; no lock call | 65 person state changes 2026-09-18 22:30 → 2026-09-21 23:07: 44 within-site hops, of which 18 pushed H to exactly hop + 20:00 (whole second, written in the same second) and 26 left H unchanged because it was already later (monotonic max); arrivals 6 pushed / 4 unchanged, departures 1 pushed / 8 unchanged, 0 missing pushes. Two writes flagged by the analysis were the next hop's push one second later (19:04:13 on 09-20, 21:23:18 on 09-21) | PASS |
| A12 | A change entirely outside the site (e.g. `not_home` → a zone elsewhere) pushes nothing | helper unchanged | not observable on Hytta: all eight configured zones lie inside the 54 m home zone, so every GPS transition is an arrival, a departure or a within-site move; covered by `test_t5_moves_entirely_outside_the_site_never_push` and the mutation "push the timer on moves outside the site too" | N/A |

Notes 2026-09-21: HA was restarted once more at 22:35:44 on 2026-09-18 (container restart only, host up since 2026-09-17 18:38; not part of the deploy); the deployed values survived. The lock was `unavailable` to HA nine times in four days (Fri 16:17–22:17, Sat 20:58–Sun 19:28, then 40/60/20/10/10/10-min windows on Mon). The gateway's own Wi-Fi tracker (`device_tracker.k27916629`) was offline only on Fri (4×, 9–29 min) and Sun 03:16–04:12 and 19:49–20:19 (the user unplugs it now and then to free a power outlet), so most lock outages happened with the gateway on the Wi-Fi; all nine recoveries coincided to the second with a lock operation (`unlock by passcode` or `lock by app`), i.e. the lock becomes queryable again when it is used. Mon 22:40–22:49 was the Pi's own Wi-Fi: it roamed to the 2.4 GHz radio and lost the LAN gateway for 9 min (netprobe), which also took the UniFi trackers `unavailable`. A settle that falls into such a window is retried by the `lock_available` trigger and the 5-minute safety net.
